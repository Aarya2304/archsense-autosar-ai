# ArchSense — application dependencies
# Python 3.11 on Windows. Install with: pip install -r requirements.txt

# --- Persistence / core ---
SQLAlchemy>=2.0,<3
Pydantic>=2.7,<3
python-dotenv>=1.0

# --- Document ingestion (M1) ---
PyMuPDF>=1.24
pdfplumber>=0.11

# --- RAG foundation (M2) ---
# Versions pinned to what the M2 milestone was built/tested against.
chromadb>=1.0,<2
sentence-transformers>=5.0,<7
numpy>=2.0,<2.3

# --- Cited RAG copilot (M3) ---
# HTTP transport for the OpenRouter/Ollama LLM providers (mock provider
# needs nothing). No other new dependencies: BM25/RRF/gate/citations are
# standard-library implementations (D-016, D-018, D-019).
requests>=2.31

# --- Dataset generation (M0) ---
reportlab>=4.0

# --- Graph + UI (M5+) ---
networkx>=3.3
pyvis>=0.3.2
streamlit>=1.37
plotly>=5.22
pandas>=2.0

# --- Dev / test ---
pytest>=8.0
