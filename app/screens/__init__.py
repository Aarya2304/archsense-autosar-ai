"""ArchSense UI screens (M8). Each module exposes ``render()``.

The package is named ``screens`` (not ``pages``) deliberately: Streamlit
auto-treats any ``pages/`` directory next to the entry script as a
multipage-app folder, which would conflict with the app's own router.
"""
